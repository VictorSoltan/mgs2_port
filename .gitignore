# Никаких бинарников: всё пересобирается из wine-patches/ по рецептам в README.
*.dll
*.so
*.so.*
*.o
*.a
*.exe

# Сборочные каталоги и исходники Wine — они восстанавливаются из тарбола плюс патчи.
build-wine-*/
wine-11.0/
*.tar.xz
*.tar.gz

# Логи и записи с устройства.
*.log
*.wav
